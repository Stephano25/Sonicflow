package com.exemple.sonicflow.player

import android.media.MediaPlayer

class CrossfadeController {

    fun crossfade(currentPlayer: MediaPlayer, nextPlayer: MediaPlayer, durationMs: Int = 3000) {
        // Implémentation à ajouter : diminuer volume du currentPlayer
        // et augmenter celui du nextPlayer progressivement
    }
}
